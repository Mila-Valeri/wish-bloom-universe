🧩 Завдання 1: Автоматичне відображення нової картки на головній сторінці без перезавантаження
📌 Проблема:
Після створення нової картки бажання користувачу доводиться вручну оновлювати сторінку, щоб побачити її на головній.

✅ Очікуване рішення:
Реалізувати автоматичне оновлення списку карток на головній сторінці після створення нової картки без необхідності ручного перезавантаження (window.location.reload()).

Використати підписку на зміни (через useState + useEffect, або WebSocket / Context API) або застосувати додавання нової картки в локальний стан після успішної відповіді API.

🌗 Завдання 2: Збереження теми (світлої/темної) при оновленні сторінки
📌 Проблема:
Після оновлення сторінки тема (світла/темна) скидається до дефолтної. Вибір користувача не зберігається.

✅ Очікуване рішення:
Зберігати вибрану тему в localStorage або sessionStorage.

При завантаженні сайту перевіряти, яка тема збережена, та автоматично застосовувати її.

При перемиканні теми — оновлювати значення в storage.

Забезпечити роботу збереженої теми на всіх сторінках, включаючи /profile, /settings, /wish.

🖼 Завдання 3: Удосконалення перегляду фото при створенні картки бажання
📌 Проблема:
Після кадрування зображення та перед підтвердженням створення картки, попередній перегляд (preview) фото виглядає обрізаним неправильно. Лише після збереження картки оброблене зображення виглядає коректно.

✅ Очікуване рішення:
Попередній перегляд фото у вікні перед підтвердженням повинен відображати фінальний кадр точно так, як він буде виглядати на картці.

При оновленні компоненту preview — використовувати обрізане та масштабоване зображення після Apply Crop, а не початковий файл.

Уточнити, чи preview використовує canvas, чи <img src> — у разі canvas застосувати актуальне зображення з crop-обробки.

➕ Додатково (суміжне з попереднім):
🧱 Реюз компонента створення картки:
Додати можливість створення картки бажання на всіх сторінках сайту, а не лише на головній.

Компонент створення картки має бути переиспользований (reuse) на сторінках:

/profile

/settings

інші вкладки (якщо є).

Всі функції створення картки, включаючи завантаження зображення, crop, preview, повинні працювати повноцінно незалежно від сторінки.

